-- 타입추론과 함수 --

fun sum(a: Int, b: Int, c: Int):Int{ 
    // 기존 함수 선언과 다른점은 변수의 형을 선언할 때, 앞이 아닌 뒤에 :를 붙여 해준다는 것이고, 두번째는 반환하는 값의 형을 지정해주는 것을 parameter 뒤에 선언해주어야 한다는 것이다.
    // 반환형이라고 한다.
    return a+b+c
}

// 단일표현식 함수(위에 것과 완전히 같은 결과)
fun add(a:Int, b:Int, c:Int) = a+b+c // 반환형의 타입추론이 가능하므로 반환형은 없어도됨

//함수는 파라미터를 넣는 동작함수이지만 코틀린에서는 자료형이 결정된 변수라는 개념으로 접근하는 것이 좋다.

fun main(){
    var num : Int = 123
    var num2 = 123
    // Kotlin은 대부분의 변수가 타입 추론이 가능하여 굳이 타입을 명시해주지 않아도 된다.
    println(sum(num,num2,3))
}